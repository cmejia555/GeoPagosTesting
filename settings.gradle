rootProject.name = 'GeoPagos'

assignProperty('browserName', 'chrome')
assignProperty('appUrl', 'https://www.mercadolibre.com.ar')
assignProperty('appDefaultWait', '10')
assignProperty('pageLoadWait', '300')
assignProperty('driverPath', '/home/cesar/Downloads/chromedriver')

def assignProperty(key, defaultValue){
    if(System.getProperty(key) == null){
        System.setProperty(key, defaultValue)
    }
}